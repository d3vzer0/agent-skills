repos:
- repo: https://github.com/astral-sh/ruff-pre-commit
  rev: v0.15.14
  hooks:
    # Run the linter.
    - id: ruff-check
      args: [ --fix, "--extend-select=I" ]
    # Run the formatter.
    - id: ruff-format